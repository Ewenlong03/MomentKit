platform :ios, '8.0'

target 'MomentKit' do
  
  pod "UMengAnalytics-NO-IDFA"
  pod "AMap3DMap"
  pod "SDWebImage"
  pod "MJRefresh"
  pod "MLLabel"
  pod "FMDB"
  pod "YYText"
  pod "MMWebView"
  pod "UUButton"
  pod "UUActionSheet"

end
